import 'package:flutter/material.dart';

class editNPCScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Create NPC"),
      ),

      body: Center(
        child: Column(
          children: <Widget>[
            Container(

            ),
          ]
        ),
      )
    );
  }
}